diff --git a/src/lib/server/repositories/tripsRepo.ts b/src/lib/server/repositories/tripsRepo.ts
index b0eb861..0547534 100644
--- a/src/lib/server/repositories/tripsRepo.ts
+++ b/src/lib/server/repositories/tripsRepo.ts
@@ -216,6 +216,12 @@ export interface CreateTripInput {
 
 export type UpdateTripInput = Partial<Omit<CreateTripInput, 'name'>> & { name?: string; updatedAt?: string };
 
+// roamarr-de: default a new trip's base currency to its owner's default currency (upstream: always 'USD').
+function ownerDefaultCurrency(ownerId: number): string {
+	const [owner] = kit.selectFrom(usersTable).where(kitEq(usersTable.id, kitId(ownerId))).executeSync();
+	return (owner?.default_currency as string | null | undefined) || 'USD';
+}
+
 export function createTrip(ownerId: number, input: CreateTripInput): Trip {
 	const row = kit
 		.insertInto(trips)
@@ -240,7 +246,7 @@ export function createTrip(ownerId: number, input: CreateTripInput): Trip {
 			public_show_details: input.publicShowDetails ?? false,
 			calendar_token: input.calendarToken ?? null,
 			calendar_token_expires_at: input.calendarTokenExpiresAt ?? null,
-			base_currency: input.baseCurrency ?? 'USD',
+			base_currency: input.baseCurrency ?? ownerDefaultCurrency(ownerId),
 			status: input.status ?? 'booked',
 			poster_attachment_id: input.posterAttachmentId != null ? kitId(input.posterAttachmentId) : null
 		} as Insert<typeof trips>)
